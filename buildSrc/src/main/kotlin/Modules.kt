object Modules {

    const val app = ":app"

    object Core {
        private const val prefix = "core"

        const val common = ":$prefix:common"
        const val design = ":$prefix:design"
    }
}